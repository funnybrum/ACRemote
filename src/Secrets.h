#ifndef Secrets_h
#define Secrets_h

// Execut 'git update-index --assume-unchanged ./src/Secrets.h' to avoid tracking changes to this file.

const char* WIFI_SSID = "wifi_network_ssid";
const char* WIFI_PASSWORD = "wifi_network_password";

#endif